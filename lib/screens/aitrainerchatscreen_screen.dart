import 'package:flutter/material.dart';

class AiTrainerChatScreen extends StatelessWidget {
  const AiTrainerChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI Trainer Chat')),
      body: const Center(child: Text('AI Trainer Chat coming soon!')),
    );
  }
}
